%h1 Categories
%br

%table
  %thead
    %tr
      %th Name
  %tbody
    - @categories.each do |category|
      %tr
        %td= category.name
        %td
          = link_to category_path(category.id) do 
            Show